extends layout

block content
    h1= title
    form(method='get')
        input(type='text' value=search name='search')
        input(type='submit' value='Search')
    
    each item in data
        article
            h1.title=item.title
            p.date=item.created
            p=item.description